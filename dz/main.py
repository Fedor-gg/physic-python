import math

# ============================================================================
# Задача 1. Пружинный маятник
# ============================================================================
print("=" * 50)
print("Задача 1. Пружинный маятник")
print("=" * 50)

# Входные данные
m = 0.2      # масса груза, кг
k = 50.0     # жёсткость пружины, Н/м
A = 0.05     # амплитуда колебаний, м

# Расчёты
T = 2 * math.pi * math.sqrt(m / k)          # период, с
nu = 1 / T                                   # частота, Гц
omega = math.sqrt(k / m)                     # циклическая частота, рад/с

# Энергия колебаний
# При максимальном отклонении вся энергия - потенциальная
E_max_potential = (k * A**2) / 2            # Дж
# При прохождении положения равновесия вся энергия - кинетическая
E_max_kinetic = E_max_potential              # Дж

# Вывод результатов
print(f"Масса груза (m) = {m} кг")
print(f"Жёсткость пружины (k) = {k} Н/м")
print(f"Амплитуда (A) = {A} м")
print()
print(f"1. Период T = {T:.4f} с")
print(f"2. Частота ν = {nu:.2f} Гц")
print(f"3. Циклическая частота ω = {omega:.2f} рад/с")
print(f"4. Максимальная кинетическая энергия = {E_max_kinetic:.4f} Дж")
print(f"   Максимальная потенциальная энергия = {E_max_potential:.4f} Дж")

# Проверка: максимальная скорость
v_max = A * omega
print(f"\n   (Проверка: максимальная скорость v_max = {v_max:.3f} м/с)")
print(f"   E_kin_max = m*v_max²/2 = {0.5 * m * v_max**2:.4f} Дж")

# ============================================================================
# Задача 2. Математический маятник
# ============================================================================
print("\n" + "=" * 50)
print("Задача 2. Математический маятник")
print("=" * 50)

# Часть А: Расчёт периода по известным L и g
L = 1.0      # длина нити, м
g = 9.81     # ускорение свободного падения, м/с²

T_calculated = 2 * math.pi * math.sqrt(L / g)

print("\n--- Часть А: Расчёт периода ---")
print(f"Длина нити (L) = {L} м")
print(f"Ускорение g = {g} м/с²")
print(f"Рассчитанный период T = {T_calculated:.4f} с")

# Часть Б: Определение g по измеренным T и L
T_measured = 2.0    # измеренный период, с
L_measured = 0.99   # измеренная длина, м

g_calculated = (4 * math.pi**2 * L_measured) / (T_measured**2)

print("\n--- Часть Б: Определение g ---")
print(f"Измеренный период T = {T_measured} с")
print(f"Измеренная длина L = {L_measured} м")
print(f"Вычисленное ускорение g = {g_calculated:.4f} м/с²")

# Относительная погрешность
g_true = 9.81
error = abs(g_calculated - g_true) / g_true * 100
print(f"Относительная погрешность: {error:.2f}%")

# ============================================================================
# Дополнительно: Сравнение и проверка формул
# ============================================================================
print("\n" + "=" * 50)
print("Дополнительная проверка")
print("=" * 50)

# Для пружинного маятника
print("\nПружинный маятник:")
print(f"  T = 2π√(m/k) = 2π√({m}/{k}) = {T:.4f} с")
print(f"  ω = √(k/m) = √({k}/{m}) = {omega:.2f} рад/с")

# Для математического маятника при малых углах
print("\nМатематический маятник:")
print(f"  Формула работает для малых углов (θ < 10°~15°)")

# Демонстрация: период не зависит от массы и амплитуды
L_demo = 0.5
g_demo = 9.81
T_demo = 2 * math.pi * math.sqrt(L_demo / g_demo)
print(f"\nПример: при L = {L_demo} м, g = {g_demo} м/с²")
print(f"  Период T = {T_demo:.4f} с")

# Сравнение периодов при разных длинах
print("\nСравнение периодов математического маятника:")
lengths = [0.25, 0.5, 1.0, 1.5, 2.0]
for L_val in lengths:
    T_val = 2 * math.pi * math.sqrt(L_val / g)
    print(f"  L = {L_val:.2f} м → T = {T_val:.4f} с")